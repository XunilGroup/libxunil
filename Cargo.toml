[package]
name = "libxunil"
version = "0.1.0"
edition = "2024"

[lib]
name = "xunil"
crate-type = ["staticlib"]

[profile.release]
panic = "abort"
opt-level = "s"

[dependencies]
spin = "0.10.0"

[profile.dev]
panic = "abort"
